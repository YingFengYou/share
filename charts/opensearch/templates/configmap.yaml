apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opensearch.fullname" . }}-config
  labels:
    {{- include "opensearch.labels" . | nindent 4 }}
data:
  opensearch.yml: |
    {{- index .Values.config "opensearch.yml" | toYaml | nindent 4 }}
